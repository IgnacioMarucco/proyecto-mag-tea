FROM node:22-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci

# --poll necesario en Windows: los volume mounts no disparan inotify
CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--poll", "2000", "--proxy-config", "proxy.conf.docker.json"]
