FROM node:24-alpine
WORKDIR /app

COPY package*.json ./
RUN npm install

# --poll necesario en Windows: los volume mounts no disparan inotify
CMD ["npx", "ng", "serve", "--host", "0.0.0.0", "--poll", "2000", "--no-hmr", "--proxy-config", "proxy.conf.docker.json"]
